#ifndef KJIHEADER_HPP
#define KJIHEADER_HPP

#include <vector>

template<typename T>
void mm_kji(T a, const std::vector<T>& A, const std::vector<T>& B, T b, std::vector<T>&C, int m, int p, int n);

#endif